[project]
name = "app"
version = "0.1.0"
description = "Personal project"
authors = [
    {name = "Alexandr Sotnikov"}
]
requires-python = ">=3.13,<3.14"
dependencies = [
    "django (>=6.1.1,<7.0.0)",
    "psycopg[binary] (>=3.3.5,<4.0.0)",
    "django-environ (>=0.14.0,<0.15.0)",
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
